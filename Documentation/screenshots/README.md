# Mobile screenshot guide

No reviewed screenshots are committed yet.

Recommended captures:

- `flight-list.png`
- `flight-detail.png`
- `shop-directory.png`
- `reservation.png`
- `notification-settings.png`

Use an emulator with synthetic data. Redact ticket numbers, JWTs, device tokens, email addresses, API URLs, and Firebase project details. Keep a consistent phone size and include captions in the main README when images are added.
